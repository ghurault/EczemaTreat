// Transformed data declaration: daily treatment usage

int idx_treat2[N_treat2]; // index of non-missing observation for treat2
int treat2[N, D_treat]; // Whether treatment was used within the past two days
int treat[N, D_treat]; // Daily treatment usage
